<?xml version="1.0" encoding="utf-8"?>
<!-- C21361681 – Michael Traynor -->
<!-- Home screen: consistent filled green buttons for all main actions -->
<!-- Only "My Profile" uses the outlined style -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@color/giggy_black"
    android:padding="24dp">

    <!-- Header -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:layout_marginBottom="8dp">

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="G"
            android:textColor="@color/giggy_green"
            android:textSize="32sp"
            android:textStyle="bold"/>

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="iggy"
            android:textColor="@color/giggy_green"
            android:textSize="32sp"
            android:textStyle="bold"/>
    </LinearLayout>

    <!-- Username -->
    <TextView
        android:id="@+id/tvUsername"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textColor="@color/giggy_text"
        android:textSize="22sp"
        android:textStyle="bold"
        android:layout_marginBottom="4dp"/>

    <!-- Role label -->
    <TextView
        android:id="@+id/tvRole"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textColor="@color/giggy_green"
        android:textSize="14sp"
        android:layout_marginBottom="24dp"/>

    <!-- My Profile - outlined (different, as requested) -->
    <Button
        android:id="@+id/btnMyProfile"
        style="@style/Widget.MaterialComponents.Button.OutlinedButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="MY PROFILE"
        android:textColor="@color/giggy_green"
        android:layout_marginBottom="24dp"/>

    <!-- Main action buttons - all filled green (same style) -->

    <!-- ARTIST buttons (shown/hidden in code) -->
    <Button
        android:id="@+id/btnBrowseGigs"
        android:layout_width="match_parent"
        android:layout_height="56dp"
        android:text="BROWSE GIGS"
        android:textColor="@color/giggy_black"
        android:background="@drawable/green_button"
        android:layout_marginBottom="12dp"
        android:visibility="gone"/>

    <Button
        android:id="@+id/btnSearchArtists"
        android:layout_width="match_parent"
        android:layout_height="56dp"
        android:text="FIND VENUES"
        android:textColor="@color/giggy_black"
        android:background="@drawable/green_button"
        android:layout_marginBottom="12dp"
        android:visibility="gone"/>

    <Button
        android:id="@+id/btnBookings"
        android:layout_width="match_parent"
        android:layout_height="56dp"
        android:text="MY APPLICATIONS"
        android:textColor="@color/giggy_black"
        android:background="@drawable/green_button"
        android:layout_marginBottom="12dp"
        android:visibility="gone"/>

    <!-- VENUE buttons (shown/hidden in code) -->
    <Button
        android:id="@+id/btnPostGig"
        android:layout_width="match_parent"
        android:layout_height="56dp"
        android:text="POST A GIG"
        android:textColor="@color/giggy_black"
        android:background="@drawable/green_button"
        android:layout_marginBottom="12dp"
        android:visibility="gone"/>

    <Button
        android:id="@+id/btnFindArtists"
        android:layout_width="match_parent"
        android:layout_height="56dp"
        android:text="FIND ARTISTS"
        android:textColor="@color/giggy_black"
        android:background="@drawable/green_button"
        android:layout_marginBottom="12dp"
        android:visibility="gone"/>

    <Button
        android:id="@+id/btnVenueBookings"
        android:layout_width="match_parent"
        android:layout_height="56dp"
        android:text="BOOKING REQUESTS"
        android:textColor="@color/giggy_black"
        android:background="@drawable/green_button"
        android:layout_marginBottom="12dp"
        android:visibility="gone"/>

    <!-- Spacer -->
    <View android:layout_width="match_parent" android:layout_height="0dp" android:layout_weight="1"/>

    <!-- Log out -->
    <Button
        android:id="@+id/btnLogout"
        style="@style/Widget.MaterialComponents.Button.TextButton"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="LOG OUT"
        android:textColor="@color/giggy_hint"/>

</LinearLayout>